# privesc bypass demo
pull_request_rules:
  - name: should-fire-but-wont-mergify-disabled
    conditions:
      - "title~=^PRIVESC"
    actions:
      label:
        add:
          - mergify-was-active
      comment:
        message: "If you see this label, Mergify is processing PRs."
