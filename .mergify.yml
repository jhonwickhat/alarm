pull_request_rules: []
merge_protections:
  - name: gate
    if: ["base = master"]
    success_conditions: ["label = approved-by-security"]
merge_protections_settings:
  reporting_method: deployments
