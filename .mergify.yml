# branch poison-target-5 diff
pull_request_rules:
  - name: poisoned-scope-detector
    conditions:
      - "scope = POISONED-CANARY-2026"
    actions:
      label:
        add:
          - mergify-poisoned-scope-fired
      comment:
        message: "Mergify fired on poisoned scope POISONED-CANARY-2026 — future-PR scope poisoning consume-side proven"
  - name: engine-alive-control
    conditions:
      - "title~=^Test"
    actions:
      label:
        add:
          - engine-alive
