# branch clean-poc-9
pull_request_rules:
  - name: poisoned-scope-detector-clean
    conditions:
      - "scope = CLEAN-CANARY-PR10"
    actions:
      label:
        add:
          - clean-poison-fired
      comment:
        message: "F-MGF-SCOPE-POISON-001 LIVE PoC: pre-pushed scope CLEAN-CANARY-PR10 fired on PR opened AFTER push. Critical bug."
