# branch asym-poc-15 diff
pull_request_rules:
  - name: asymmetric-poc-detector
    conditions:
      - "scope = ASYMMETRIC-POC-PR15"
    actions:
      label:
        add:
          - asymmetric-poc-fired
      comment:
        message: "F-MGF-SCOPE-POISON-001: scope ASYMMETRIC-POC-PR15 was pushed by abredhat (attacker w/ leaked ci key, NO GH access) BEFORE PR #15 existed. abredhat could not have done this without exploiting the bug."
